# Force npm as package manager
# Prevent Next.js from trying to use pnpm
package-manager=npm

# Always use legacy peer dependency resolution (prevents ERESOLVE when Next
# déclenche des npm install internes sans ce flag)
legacy-peer-deps=true
