// This experimental layout is not used anymore. Keep a noop export to satisfy imports if any.
export function MinimalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
